pipeline {
    agent any
    stages {
        stage('Git Clone') {
            steps {
                echo "Starting build ...."
                    sh '''#!/bin/bash
                          npm -v
                     '''
            }
        }
    }
}
